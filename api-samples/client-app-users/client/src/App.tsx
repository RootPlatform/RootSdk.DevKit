// ============================================================================
// API Sample: Client Users — App Root
// ============================================================================

import React from "react";
import { UserCard } from "./UserCard";

export const App: React.FC = () => {
  return (
    <div>
      <UserCard />
    </div>
  );
};
