---
path: app-api-reference/client/type-aliases/RootThemeMode.md
audience: app
category: reference
summary: The host Root client's current theme: `"light"` or `"dark"`. Returned by `rootClient.theme.getTheme()` and passed to `theme.update` event listeners.
---

> **RootThemeMode** = `"light"` | `"dark"`

The host Root client's current theme: `"light"` or `"dark"`. Returned by `rootClient.theme.getTheme()` and passed to `theme.update` event listeners.