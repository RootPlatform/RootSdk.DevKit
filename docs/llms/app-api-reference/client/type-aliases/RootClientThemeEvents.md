---
path: app-api-reference/client/type-aliases/RootClientThemeEvents.md
audience: app
category: reference
summary: Event map type for `RootClientTheme`. This type defines the event signatures for theme events.
---

> **RootClientThemeEvents** = `object`

Event map type for `RootClientTheme`. This type defines the event signatures for theme events.

For event name constants, see `RootClientThemeEvent`.

## Properties

### theme.update()

> **theme.update**: (`event`: [`RootThemeMode`](RootThemeMode.md)) => `void`

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | [`RootThemeMode`](RootThemeMode.md) |

#### Returns

`void`