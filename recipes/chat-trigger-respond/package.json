{
  "name": "@rootsdk-recipe/chat-trigger-respond",
  "author": "Root Communications, Inc.",
  "description": "Recipe: respond to user chat messages with a slash-command-style trigger. Server-only — no UI, no proto. The smallest recipe shape.",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@rootsdk/server-app": "0.17.1-alpha.6"
  },
  "devDependencies": {
    "@rootsdk/dev-tools": "0.17.1-alpha.6",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "node clean.js"
  },
  "workspaces": {
    "packages": [
      "./server"
    ]
  }
}
