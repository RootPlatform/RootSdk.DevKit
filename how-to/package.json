{
  "name": "@rootsdk-howto/root",
  "private": true,
  "scripts": {
    "clean": "node scripts/foreach.js clean",
    "install:all": "node scripts/foreach.js install",
    "build": "node scripts/foreach.js build"
  }
}
